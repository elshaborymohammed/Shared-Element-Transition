<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph"
    app:startDestination="@id/fragment_movie_list">

    <fragment
        android:id="@+id/fragment_movie_list"
        android:name="com.smart.sample.fragments.FragmentMovieList"
        android:label="fragment_movie_list"
        tools:layout="@layout/fragment_movie_list">

        <action
            android:id="@+id/action_to_movie_detail"
            app:destination="@id/fragment_movie_detail" />
    </fragment>
    <fragment
        android:id="@+id/fragment_movie_detail"
        android:name="com.smart.sample.fragments.FragmentMovieDetail"
        android:label="fragment_movie_detail"
        tools:layout="@layout/fragment_movie_detail" />
</navigation>